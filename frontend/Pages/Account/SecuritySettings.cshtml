@page "/security"
@model BoostingHub.frontend.Pages.Account.SecuritySettingsModel
@{
    ViewData["Title"] = "Security Settings";
    ViewData["BodyClass"] = "user-dashboard-page";
}

<div class="admin-layout">
    <aside class="admin-sidebar @(HttpContext.Session.GetString("UserRole") != "Admin" ? "user-sidebar" : "")">
        <div class="sidebar-brand">
            <div class="sidebar-brand-icon"><i class="bi bi-rocket-takeoff"></i></div>
            <span>Boosting Hub</span>
        </div>
        <nav class="sidebar-nav">
            @if (HttpContext.Session.GetString("UserRole") == "Admin")
            {
                <a href="/admin-panel" class="sidebar-link"><i class="bi bi-speedometer2"></i> Dashboard</a>
                <a href="/admin/inquiries" class="sidebar-link"><i class="bi bi-inbox"></i> Client Requirements</a>
                <div class="sidebar-divider"></div>
                <a href="/security" class="sidebar-link active"><i class="bi bi-shield-lock"></i> Security</a>
            }
            else
            {
                <a href="/dashboard" class="sidebar-link"><i class="bi bi-speedometer2"></i> Dashboard</a>
                <a href="/tasks" class="sidebar-link"><i class="bi bi-list-task"></i> Available Tasks</a>
                <a href="/my-tasks" class="sidebar-link"><i class="bi bi-check2-square"></i> My Tasks</a>
                <a href="/security" class="sidebar-link active"><i class="bi bi-shield-lock"></i> Security</a>
            }
            <div class="sidebar-divider"></div>
            <a href="/logout" class="sidebar-link sidebar-logout"><i class="bi bi-box-arrow-right"></i> Logout</a>
        </nav>
    </aside>
    <main class="admin-main">
        <div class="admin-header">
            <h3><i class="bi bi-shield-lock text-primary"></i> Security Settings</h3>
            <div class="admin-header-right">
                <div class="user-profile-dropdown" id="profileDropdown">
                    <span class="admin-user-badge" onclick="toggleProfileMenu()">
                        <i class="bi bi-person-circle"></i> @(HttpContext.Session.GetString("UserRole") == "Admin" ? "Admin" : Model.CurrentUserName)
                        <i class="bi bi-chevron-down" style="font-size:0.65rem;margin-left:2px;"></i>
                    </span>
                    <div class="profile-menu" id="profileMenu" style="display:none;">
                        <a href="/logout" class="profile-menu-item"><i class="bi bi-box-arrow-right"></i> Logout</a>
                    </div>
                </div>
            </div>
        </div>
        <div class="admin-content">
            @if (!string.IsNullOrEmpty(Model.SuccessMessage))
            {
                <div class="auth-alert auth-alert-success mb-4">@Model.SuccessMessage</div>
            }
            <div class="row">
                <div class="col-md-6">
                    <div class="admin-card">
                        <div class="admin-card-header">
                            <h5><i class="bi bi-key text-primary"></i> Change Password</h5>
                        </div>
                        <div class="admin-card-body">
                            <form method="post" asp-page-handler="ChangePassword">
                                <div asp-validation-summary="ModelOnly" class="auth-alert auth-alert-danger" style="display:none;"></div>
                                <div class="input-group-custom">
                                    <label asp-for="ChangePassword.CurrentPassword" class="form-label">Current Password</label>
                                    <div class="input-icon-wrapper">
                                        <input asp-for="ChangePassword.CurrentPassword" class="form-control" type="password" placeholder="Enter current password" />
                                        <i class="input-icon bi bi-lock"></i>
                                    </div>
                                </div>
                                <div class="input-group-custom">
                                    <label asp-for="ChangePassword.NewPassword" class="form-label">New Password</label>
                                    <div class="input-icon-wrapper">
                                        <input asp-for="ChangePassword.NewPassword" class="form-control" type="password" placeholder="Enter new password" />
                                        <i class="input-icon bi bi-lock-fill"></i>
                                    </div>
                                </div>
                                <div class="input-group-custom">
                                    <label asp-for="ChangePassword.ConfirmNewPassword" class="form-label">Confirm New Password</label>
                                    <div class="input-icon-wrapper">
                                        <input asp-for="ChangePassword.ConfirmNewPassword" class="form-control" type="password" placeholder="Repeat new password" />
                                        <i class="input-icon bi bi-lock-fill"></i>
                                    </div>
                                </div>
                                <button type="submit" class="btn btn-auth w-100"><i class="bi bi-arrow-repeat"></i> Change Password</button>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        function toggleProfileMenu() {
            var menu = document.getElementById('profileMenu');
            menu.style.display = menu.style.display === 'none' ? 'block' : 'none';
        }
        document.addEventListener('click', function(e) {
            var pd = document.getElementById('profileDropdown');
            if (pd && !pd.contains(e.target)) {
                var m = document.getElementById('profileMenu');
                if (m) m.style.display = 'none';
            }
        });
    </script>
}
