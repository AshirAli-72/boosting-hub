@page "/verify-email"
@model BoostingHub.frontend.Pages.Account.VerifyEmailModel
@{
    ViewData["Title"] = "Verify Email";
    ViewData["PageType"] = "auth";
    ViewData["BodyClass"] = "auth-page-body";
}

<div class="auth-page">
    <div class="auth-card auth-card-centered">
        @if (Model.Verified)
        {
            <div class="auth-card-icon auth-card-icon-success auth-icon-centered"><i class="bi bi-check-lg"></i></div>
            <h3 class="auth-form-title text-center">Email Verified!</h3>
            <p class="auth-text-description">Your email has been verified successfully.</p>
            <a href="/login" class="btn btn-auth"><i class="bi bi-box-arrow-in-right"></i> Sign In</a>
        }
        else if (Model.Error)
        {
            <div class="auth-card-icon auth-card-icon-danger auth-icon-centered"><i class="bi bi-x-lg"></i></div>
            <h3 class="auth-form-title text-center">Verification Failed</h3>
            <p class="auth-text-description">@Model.ErrorMessage</p>
            <a href="/" class="btn btn-auth"><i class="bi bi-house"></i> Go Home</a>
        }
        else
        {
            <div class="spinner-border text-primary mb-3" role="status"></div>
            <p class="auth-text-loading">Verifying your email...</p>
        }
    </div>
</div>
