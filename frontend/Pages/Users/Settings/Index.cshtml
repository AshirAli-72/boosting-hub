@page "/user/settings"
@model BoostingHub.frontend.Pages.Users.Settings.IndexModel
@{
    ViewData["Title"] = "Settings";
    ViewData["BodyClass"] = "user-dashboard-page";
}

        <div class="admin-header">
            <h3><i class="bi bi-gear text-primary"></i> Settings</h3>
            <div class="admin-header-right">
                <button id="themeToggle" class="btn btn-sm btn-outline-primary me-3 border-0" aria-label="Toggle Theme">
                    <i class="bi bi-moon-stars fs-5"></i>
                </button>
                <div class="user-profile-dropdown" id="profileDropdown">
                    <span class="admin-user-badge" onclick="toggleProfileMenu()">
                        <i class="bi bi-person-circle"></i> User
                        <i class="bi bi-chevron-down profile-dropdown-caret"></i>
                    </span>
                    <div class="profile-menu hidden" id="profileMenu">
                        <a href="/logout" class="profile-menu-item"><i class="bi bi-box-arrow-right"></i> Logout</a>
                    </div>
                </div>
            </div>
        </div>
        <div class="admin-content">
            <div class="row g-4">
                <div class="col-md-4">
                    <a href="/settings" class="text-decoration-none">
                        <div class="card shadow-sm border-0 h-100 settings-card-clickable">
                            <div class="card-body text-center py-5">
                                <div class="settings-card-icon icon-color-purple"><i class="bi bi-shield-lock"></i></div>
                                <h5 class="fw-semibold">Security</h5>
                                <p class="text-muted mb-0">Password and profile preferences</p>
                            </div>
                        </div>
                    </a>
                </div>
                <div class="col-md-4">
                    <a href="/user/social-accounts" class="text-decoration-none">
                        <div class="card shadow-sm border-0 h-100 settings-card-clickable">
                            <div class="card-body text-center py-5">
                                <div class="settings-card-icon icon-color-blue"><i class="bi bi-globe2"></i></div>
                                <h5 class="fw-semibold">Social Accounts</h5>
                                <p class="text-muted mb-0">Link your social media accounts</p>
                            </div>
                        </div>
                    </a>
                </div>
                <div class="col-md-4">
                    <a href="/user/accounts" class="text-decoration-none">
                        <div class="card shadow-sm border-0 h-100 settings-card-clickable">
                            <div class="card-body text-center py-5">
                                <div class="settings-card-icon icon-color-teal"><i class="bi bi-credit-card"></i></div>
                                <h5 class="fw-semibold">Accounts</h5>
                                <p class="text-muted mb-0">Manage your payment accounts</p>
                            </div>
                        </div>
                    </a>
                </div>
            </div>
        </div>

@section Scripts {
    <script>
        function toggleProfileMenu() {
            var menu = document.getElementById('profileMenu');
            menu.style.display = menu.style.display === 'block' ? 'none' : 'block';
        }
        document.addEventListener('click', function(e) {
            var pd = document.getElementById('profileDropdown');
            if (pd && !pd.contains(e.target)) {
                var m = document.getElementById('profileMenu');
                if (m) m.style.display = 'none';
            }
        });
    </script>
}
